<UserControl x:Class="FlightGearTestExec.Views.FeaturesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightGearTestExec.Views"
             xmlns:viewModels="clr-namespace:FlightGearTestExec.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewModels:FeaturesListViewModel/>
    </UserControl.DataContext>
    
    <ListView Name="FeaturesList" SelectionChanged ="List_Selected">
        <ListView.View>
            <GridView>
                <GridViewColumn Width="100" Header="Features" DisplayMemberBinding="{Binding Path=FeatureName}"/>
                <GridViewColumn Width="150"  Header="Correlated Feature" DisplayMemberBinding="{Binding Path=CorrelatedName}"/>
            </GridView>
        </ListView.View>
    </ListView>

</UserControl>
